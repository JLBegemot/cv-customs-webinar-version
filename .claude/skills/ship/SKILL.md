---
name: ship
description: >
  Отгрузить готовые изменения: ветка, коммит, пуш и готовый текст описания MR.
  Использовать, когда работа закончена и её надо отправить на ревью — «отгрузи»,
  «сделай коммит и пуш», «оформи MR», а также как финальный шаг /api-tests.
---

# Ship — отгрузка изменений

Детерминированная последовательность: ветка → коммит → пуш → текст MR.

**PR этот скилл не создаёт.** Платформа — GitHub
(`github.com/JLBegemot/cv-customs-webinar-version`), но `gh` в системе
не установлен. Скилл доводит до запушенной ветки и печатает готовое
описание — PR создаёт человек по ссылке из вывода `git push`. Появится
настроенный `gh` — PR можно создавать через `gh pr create`, подставив
этот же текст в `--body`.

## Шаг 1. Показать, что отгружаем

```bash
git status --short
git diff --stat
```

Покажи пользователю список файлов. Незапланированные файлы в диффе (мусор, временные
файлы, чужие правки) — **остановиться и спросить**, а не коммитить.

`.claude/tmp/` в коммит не идёт: это рабочее состояние пайплайна.

## Шаг 2. Ветка

Уже на ветке не `main` — работать на ней. Иначе создать:

```bash
git switch -c <тип>/<краткое-описание>
```

Префикс по содержимому: `tests/` — только тесты, `feature/` — новая функциональность,
`fix/` — исправление. Описание — латиницей, через дефис, без номера задачи (трекера нет).

**На `main` не коммитить.**

## Шаг 3. Прогнать проверки

Перед коммитом — то же, что гоняет CI:

```bash
uv run pytest -q
uv run python scripts/openapi_snapshot.py --check
```

Красное — **не коммитить**. Показать вывод пользователю и остановиться.

## Шаг 4. Коммит

```bash
git add <конкретные файлы>
git commit -m "<сообщение>"
```

`git add -A` не использовать — добавлять перечисленные файлы. Сообщение: одна строка
по-русски, что сделано и зачем, без списка файлов.

## Шаг 5. Пуш

```bash
git push -u origin HEAD
```

Вывод `git push` показать пользователю целиком: GitHub печатает в нём ссылку
`Create a pull request` — это то, что человеку нужно кликнуть.

## Шаг 6. Описание PR

Собрать текст и **напечатать его в ответе** — не в файл, чтобы можно было скопировать.

Для тестового PR формат задан в
[`api-tests/references/mr-comment.md`](../api-tests/references/mr-comment.md).
Для остальных — коротко: что сделано, зачем, что проверено.

В описании только факты из фактического прогона. Не было прогона — не писать его результат.

## Правила

- Коммит и пуш — только когда пользователь этого попросил (в том числе через `/api-tests`,
  где `/ship` — последний шаг).
- Красные тесты не отгружаются.
- PR не создаётся автоматически, пока `gh` не установлен; появится —
  создавать только по явной просьбе пользователя.
- Force-push и перезапись чужих веток — запрещены.
